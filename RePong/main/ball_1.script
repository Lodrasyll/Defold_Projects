function init(self)
	-- 让球自动左右移动测试
	self.time = 0
end

function update(self, dt)
	self.time = self.time + dt
	local pos = go.get_position()
	pos.x = 300 + math.sin(self.time * 2) * 200  -- 左右摆动
	go.set_position(pos)
end